import tensorflow as tf
#blindfolding the neural net is good bc it leads to less overfitting down the line
#the loss function tells the neural network its flaws (i.e. when it fails)
#ideas: it may be easier to predict when the person will have a cardiac arrest, rather than if they will have one
